twat-fs should register an additional CLI tool `2url` that is equivalent to `twat-fs upload` with the same API.  

For the `upload` command, default behavior should only return to stdout the URL, nothing else. Only with --verbose it should print additional info. If --check is given, after the upload, it should download the file and check if it is the same as the original.